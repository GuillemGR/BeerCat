<%= form_with(model: cathegory, local: true) do |form| %>
  <% if cathegory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cathegory.errors.count, "error") %> prohibited this cathegory from being saved:</h2>

      <ul>
      <% cathegory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :cathegory_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
